# Red Hat Enterprise Linux 7 Security Guide
# section 6.5.1

# enables and disables the Audit system or locks its configuration
# When 0 is passed, this can be used to temporarily disable auditing. When 1
# is passed as an argument, it will enable auditing. To lock the audit
# configuration so that it can't be changed, pass a 2 as the argument
-e 2
